import Logo from "../_assets/us-review-logo.png";

const SiteBrand = () => {
  return (
    <div className="site-brand">
      <img className="site-brand--logo" src={Logo} alt="#" />
    </div>
  );
};

export default SiteBrand;
